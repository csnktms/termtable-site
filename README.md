# Termtable site

Push this repo to GitHub, then enable Pages: **Settings → Pages → Source: Deploy from a branch → `main` / root**. The site serves `index.html`; use the resulting URL as the App Store Support URL (`…/#support`).
